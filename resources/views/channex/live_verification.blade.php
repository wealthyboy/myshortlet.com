<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="robots" content="noindex,nofollow,noarchive">
    <title>Channex Live Verification | Avenue Montaigne</title>
    <style>
        :root { --ink:#182137; --muted:#697386; --line:#e4e8ef; --paper:#fff; --soft:#f5f7fa; --brand:#9c27b0; --green:#168149; --green-bg:#e8f7ee; --red:#bd3333; --red-bg:#fdeaea; --amber:#a76b09; --amber-bg:#fff4dc; }
        * { box-sizing:border-box; }
        body { background:#f1f3f7; color:var(--ink); font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Arial,sans-serif; margin:0; }
        .topbar { background:#161a35; color:#fff; }
        .topbar-inner { align-items:center; display:flex; justify-content:space-between; margin:auto; max-width:1320px; padding:18px 24px; }
        .brand { align-items:center; display:flex; gap:12px; }
        .brand-mark { align-items:center; background:#fff; border-radius:12px; color:#b21424; display:flex; font-size:21px; font-weight:800; height:42px; justify-content:center; width:42px; }
        .brand strong { display:block; font-size:15px; }
        .brand span { color:#b7bed1; display:block; font-size:11px; margin-top:2px; }
        .topbar-note { color:#c5cbda; font-size:12px; }
        main { margin:34px auto 60px; max-width:1320px; padding:0 24px; }
        .hero { align-items:flex-start; display:flex; gap:24px; justify-content:space-between; margin-bottom:24px; }
        .eyebrow { color:var(--brand); font-size:11px; font-weight:800; letter-spacing:.09em; margin:0 0 8px; text-transform:uppercase; }
        h1 { font-size:30px; letter-spacing:-.025em; margin:0 0 8px; }
        .hero p:last-child { color:var(--muted); font-size:14px; margin:0; }
        .status { border-radius:999px; flex:none; font-size:12px; font-weight:800; padding:10px 16px; text-transform:uppercase; }
        .status.ready,.pill.pass { background:var(--green-bg); color:var(--green); }
        .status.not-ready,.pill.fail { background:var(--red-bg); color:var(--red); }
        .card { background:var(--paper); border:1px solid var(--line); border-radius:16px; box-shadow:0 10px 30px rgba(30,40,70,.04); margin-bottom:20px; overflow:hidden; }
        .filter { align-items:flex-end; display:flex; gap:12px; padding:18px; }
        .field { flex:1; }
        label { color:#555f72; display:block; font-size:10px; font-weight:800; letter-spacing:.06em; margin-bottom:6px; text-transform:uppercase; }
        select { background:#fff; border:1px solid #d8dee8; border-radius:9px; color:#30394b; font-size:13px; height:44px; padding:0 12px; width:100%; }
        button { background:var(--brand); border:0; border-radius:999px; color:#fff; cursor:pointer; font-size:13px; font-weight:700; min-height:44px; padding:0 20px; }
        button:hover { filter:brightness(.94); }
        .summary { display:grid; gap:14px; grid-template-columns:repeat(4,minmax(0,1fr)); margin-bottom:20px; }
        .stat { background:#fff; border:1px solid var(--line); border-radius:14px; padding:17px; }
        .stat span { color:#818a99; display:block; font-size:10px; font-weight:800; letter-spacing:.05em; margin-bottom:7px; text-transform:uppercase; }
        .stat strong { display:block; font-size:13px; line-height:1.5; overflow-wrap:anywhere; }
        .card-head { align-items:center; background:#f8f9fb; border-bottom:1px solid var(--line); display:flex; justify-content:space-between; padding:14px 18px; }
        .card-head h2 { font-size:14px; margin:0; }
        .card-head small { color:#858e9d; font-size:11px; }
        .checks { display:grid; grid-template-columns:repeat(2,minmax(0,1fr)); padding:7px 17px; }
        .check { align-items:flex-start; border-bottom:1px solid #edf0f4; display:flex; gap:9px; min-height:48px; padding:13px 5px; }
        .check:nth-last-child(-n+2) { border-bottom:0; }
        .check-icon { align-items:center; border-radius:50%; display:flex; flex:0 0 19px; font-size:12px; font-weight:900; height:19px; justify-content:center; }
        .check.pass .check-icon { background:var(--green-bg); color:var(--green); }
        .check.fail .check-icon { background:var(--red-bg); color:var(--red); }
        .check.warning .check-icon { background:var(--amber-bg); color:var(--amber); }
        .check span:last-child { color:#4d5667; font-size:12px; line-height:1.55; }
        .table-wrap { overflow-x:auto; }
        table { border-collapse:collapse; min-width:900px; width:100%; }
        th { background:#f8f9fb; color:#30394a; font-size:10px; font-weight:800; letter-spacing:.05em; padding:13px 16px; text-align:left; text-transform:uppercase; }
        td { border-top:1px solid #edf0f4; color:#4d5665; font-size:12px; padding:14px 16px; vertical-align:middle; }
        td strong { color:#242c3a; font-size:13px; }
        code { background:#f1f3f6; border-radius:6px; color:#495366; display:inline-block; font-size:11px; padding:5px 7px; white-space:nowrap; }
        .pill { border-radius:999px; display:inline-block; font-size:9px; font-weight:800; padding:5px 9px; text-transform:uppercase; }
        .email { padding:18px; }
        textarea { background:#fafbfc; border:1px solid #dce1e8; border-radius:10px; color:#3f4858; font:12px/1.65 inherit; min-height:390px; padding:15px; resize:vertical; width:100%; }
        .email-actions { align-items:center; display:flex; gap:16px; justify-content:space-between; margin-top:12px; }
        .email-actions small { color:#7f8897; font-size:11px; }
        .empty { color:#747d8c; padding:50px 20px; text-align:center; }
        .disclaimer { color:#7e8796; font-size:11px; margin-top:-6px; text-align:center; }
        @media(max-width:900px){ .summary{grid-template-columns:repeat(2,minmax(0,1fr));}.checks{grid-template-columns:1fr}.check:nth-last-child(-n+2){border-bottom:1px solid #edf0f4}.check:last-child{border-bottom:0}.topbar-note{display:none} }
        @media(max-width:600px){ main{padding:0 14px}.topbar-inner{padding:15px}.hero{flex-direction:column}.summary{grid-template-columns:1fr}.filter{align-items:stretch;flex-direction:column}.field,button{width:100%}h1{font-size:25px}.email-actions{align-items:stretch;flex-direction:column}.email-actions button{width:100%} }
    </style>
</head>
<body>
    <header class="topbar">
        <div class="topbar-inner">
            <div class="brand"><div class="brand-mark">AM</div><div><strong>Avenue Montaigne</strong><span>Integration diagnostics</span></div></div>
            <div class="topbar-note">Public read-only verification · no credentials displayed</div>
        </div>
    </header>

    <main>
        <div class="hero">
            <div><p class="eyebrow">Channex integration</p><h1>Live verification report</h1><p>Checks mappings, UUIDs, queue health and remote entities without modifying or synchronising data.</p></div>
            @if($report)<span class="status {{ $report['ready'] ? 'ready' : 'not-ready' }}">{{ $report['ready'] ? 'Ready' : 'Not ready' }}</span>@endif
        </div>

        <form method="GET" action="{{ route('channex.live_verification') }}" class="card filter">
            <div class="field"><label for="property_id">Test property</label><select id="property_id" name="property_id">
                @foreach($properties as $option)<option value="{{ $option->id }}" {{ $property && $property->id === $option->id ? 'selected' : '' }}>#{{ $option->id }} — {{ $option->name }}</option>@endforeach
            </select></div>
            <button type="submit">Refresh status</button>
        </form>

        @if(!$property || !$report)
            <section class="card empty"><strong>No mapped Channex test property was found on this server.</strong></section>
        @else
            <div class="summary">
                <div class="stat"><span>Local property</span><strong>#{{ $property->id }} — {{ $property->name }}</strong></div>
                <div class="stat"><span>Property UUID</span><strong>{{ $property->channex_property_id ?: 'Not mapped' }}</strong></div>
                <div class="stat"><span>Room records</span><strong>{{ $report['rooms']->count() }}</strong></div>
                <div class="stat"><span>Checked</span><strong>{{ $report['checked_at']->format('d M Y, H:i:s') }}</strong></div>
            </div>

            <section class="card">
                <div class="card-head"><h2>Verification checks</h2><small>{{ $report['failures'] }} failure(s) · {{ $report['warnings'] }} warning(s)</small></div>
                <div class="checks">@foreach($report['checks'] as $check)<div class="check {{ $check['status'] }}"><span class="check-icon">{{ $check['status'] === 'pass' ? '✓' : ($check['status'] === 'warning' ? '!' : '×') }}</span><span>{{ $check['message'] }}</span></div>@endforeach</div>
            </section>

            <section class="card">
                <div class="card-head"><h2>Room and rate-plan UUIDs</h2><small>Read from the deployed database</small></div>
                <div class="table-wrap"><table><thead><tr><th>Room</th><th>Room UUID</th><th>Rate plan</th><th>Default rate</th><th>Rate-plan UUID</th><th>Status</th></tr></thead><tbody>
                @forelse($report['rooms'] as $room)
                    @php($plans = $room->channexRatePlans->where('is_active', true))
                    @if($plans->isEmpty())<tr><td><strong>#{{ $room->id }} {{ $room->name }}</strong></td><td><code>{{ $room->channex_room_type_id ?: 'Not mapped' }}</code></td><td colspan="3">No active rate plan</td><td><span class="pill fail">Incomplete</span></td></tr>
                    @else @foreach($plans as $plan)<tr><td><strong>#{{ $room->id }} {{ $room->name }}</strong></td><td><code>{{ $room->channex_room_type_id ?: 'Not mapped' }}</code></td><td>{{ $plan->name }}</td><td>USD {{ number_format((float)$plan->default_rate,2) }}</td><td><code>{{ $plan->channex_rate_plan_id ?: 'Not mapped' }}</code></td><td><span class="pill {{ $room->channex_synced && $plan->channex_rate_plan_id ? 'pass' : 'fail' }}">{{ $room->channex_synced && $plan->channex_rate_plan_id ? 'Mapped' : 'Check' }}</span></td></tr>@endforeach @endif
                @empty <tr><td colspan="6">No rooms found.</td></tr> @endforelse
                </tbody></table></div>
            </section>

            @if(count($report['remote']))<section class="card"><div class="card-head"><h2>Remote Channex status</h2><small>No data was changed</small></div><div class="table-wrap"><table><thead><tr><th>Entity</th><th>Name</th><th>UUID</th><th>HTTP</th><th>Result</th></tr></thead><tbody>
                @foreach($report['remote'] as $entity)<tr><td>{{ $entity['type'] }}</td><td><strong>{{ $entity['name'] }}</strong></td><td><code>{{ $entity['uuid'] }}</code></td><td>{{ $entity['http_status'] ?: '—' }}</td><td><span class="pill {{ $entity['exists'] ? 'pass' : 'fail' }}">{{ $entity['exists'] ? 'Confirmed' : 'Failed' }}</span></td></tr>@endforeach
            </tbody></table></div></section>@endif

            <section class="card"><div class="card-head"><h2>Copy-ready email for Channex</h2><small>Add the video link before sending</small></div><div class="email"><textarea id="channex-email" readonly>{{ $report['email'] }}</textarea><div class="email-actions"><small id="copy-status">Generated from the UUIDs currently saved on this server.</small><button type="button" id="copy-email">Copy email</button></div></div></section>
            <p class="disclaimer">For safety, this page never displays API keys, webhook secrets, credentials or raw remote responses. Results are cached for one minute.</p>
        @endif
    </main>
    <script>
        (function(){var b=document.getElementById('copy-email'),e=document.getElementById('channex-email'),s=document.getElementById('copy-status');if(!b||!e)return;b.addEventListener('click',function(){var done=function(){s.textContent='Email copied. Add your video link before sending.';b.textContent='Copied';};if(navigator.clipboard&&window.isSecureContext){navigator.clipboard.writeText(e.value).then(done);return;}e.removeAttribute('readonly');e.select();document.execCommand('copy');e.setAttribute('readonly','readonly');done();});}());
    </script>
</body>
</html>
